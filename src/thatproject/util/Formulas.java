package thatproject.util;

public class Formulas {

    /*
    String maxExp2, damage3;
    double maxExp, attack2, damage2, dexterity2, avoid, accuracy, block, critical;
    int hp, maxExp3, maxHp, attack, damage, strength, dexterity, endurance, level, mLevel, defence, mDamage, mHp, mDefence, stamina, maxStamina, luck;
    maxHp = 50 + strength + endurance * 5 + level * 8;
    maxExp = 100 * Math.pow(1.15, level);
    maxExp2 = Double.toString(maxExp);
    maxExp3 = Integer.parseInt(maxExp2);
    attack = 2 + strength * 3;
    attack2 = attack;
    dexterity2 = dexterity;
    avoid = (level * 2 + dexterity * 3)/mLevel;
    defence = level * 2 + endurance * 3 + strength;
    accuracy = 100 - (level - mLevel) * 10;
    block = (level * 2 + dexterity *2)/(mLevel*1.5);
    maxStamina = 25 + level * 5 + endurance * 4 + strength;
    critical = 4 + luck * 1;
    
    
    
    
    if (Math.random()*100 < avoid) {
        if (Math.random() <= block) {
            hp-=(mDamage-defence/8)/2;
        }
        else hp-=(mDamage-defence/8);
    }
    
    if (Math.random()*100 <= accuracy) {
        if (Math.random()*100 <= critical) {
            damage2 = Math.random()*attack + dexterity2;
            damage3 = Double.toString(damage2);
            damage = Integer.parseInt(damage3);
            mHp-=(damage-mDefence/8)*2;
        }
        else {
            damage2 = Math.random()*attack + dexterity2;
            damage3 = Double.toString(damage2);
            damage = Integer.parseInt(damage3);
            mHp-=(damage-mDefence/8);
        }
    }
    */
}
